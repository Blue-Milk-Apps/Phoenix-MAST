[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "appcritiq-core"
version = "0.1.0"
description = "All-in-one MobSec MAST tool for iOS and Android"
requires-python = ">=3.12"
dependencies = [
    "androguard>=4.1.3",
    "apkid==3.1.0",
    "jinja2>=3.1.6",
    "lief",
    "markupsafe>=3.0.3",
    "matplotlib>=3.10.9",
    "numpy>=2.4.5",
    "weasyprint>=69.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.0",
    "pytest>=8.0",
    "ruff>=0.15.15",
]

[project.scripts]
appcritiq = "entrypoints.cli:main"

[tool.setuptools]
packages = [
    "adapters",
    "adapters.binary_scanners",
    "adapters.output",
    "adapters.output.appcritiq_report",
    "adapters.post_scan",
    "adapters.storage",
    "adapters.source_code_scanners",
    "application",
    "domain",
    "entrypoints",
    "ports",
    "utilities",
]

[tool.setuptools.package-data]
"adapters.output.appcritiq_report" = [
    "assets/*",
    "data/*",
    "docs/*",
    "schema/*",
    "templates/*",
]


[tool.pytest.ini_options]
testpaths = [
    "tests",
]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
extend-select = ["I"]
